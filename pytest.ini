[pytest]

norecursedirs =
    .git
    .tox
    .env
    .eggs
    dist
    build
    south_migrations
    migrations
    docker
    perl
    r

python_files =
    test_*.py
    *_test.py
    tests.py

addopts =
    -rxEfsw
    --strict-markers
    --doctest-modules
    --doctest-glob=\*.rst
    --tb=short
    --ignore=setup.py,register_apps
    -p no:typeguard
